'use strict';

/**
 * estado-residuo controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::estado-residuo.estado-residuo');
